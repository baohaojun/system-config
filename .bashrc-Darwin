#!/usr/bin/env bash

export HOMEBREW_BOTTLE_DOMAIN=https://mirrors.tuna.tsinghua.edu.cn/homebrew-bottles
if test -e /usr/local/etc/bash_completion.d/brew; then
    . /usr/local/etc/bash_completion.d/brew
fi
