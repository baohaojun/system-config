#include <stdio.h>

int * init_fail_over(char* str)
{
    int *skip_over = (int *)malloc(sizeof int, strlen(str) + 1);
    int i;

    for (i = 1; str[i]; i++) {
        if (str[i] == str[0]) {

}
int main(int argc, char* argv[])
{
}
