#!/usr/bin/env bash

# This line will be automatically uncommented when you "make install"
#installed=1

if [ -z $installed ]; then
    CMD="./beagrep-info"
else
    CMD="beagrep-info"
fi

while true; do
    clear
    echo -n "beagrep-status at "
    date
    $CMD --status
    sleep 5
done

