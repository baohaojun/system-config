#!/bin/bash

dir=/sdcard/DCIM/Camera
if echo "$1" | grep s -i; then
    dir=/sdcard/Pictures/Screenshots/
elif echo "$1" | grep d -i; then
    dir=/sdcard/Download/
fi

pic=$(adb busybox ls -t -1 $dir|head -n 1|perl -npe 's/\033.*?m//g')
cd ~/Pictures/
adb pull $dir/$pic
up $pic
of $pic
