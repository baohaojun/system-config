#!/usr/bin/env perl
system("adb shell sync; adb shell mount -o remount rw /system");
$dest = pop( @ARGV);
for (@ARGV) {
  chomp($file=qx/lookup_file $_/);
  
  system("adb push $file $dest");
}
system("adb shell sync; adb shell mount -o remount ro /system");
