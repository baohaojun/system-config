#!/usr/bin/env perl

$arg1 = $ARGV[0];
$arg2 = $ARGV[1];

@arg1 = split(/\s|'/, $arg1);
@arg2 = split(/\s|'/, $arg2);

for (@arg2) {
    $arg2{$_} = 1;
}

for (@arg1) {
    $arg1{$_} = 1 unless $arg2{$_} == 1;
}

$,="\n";
print keys %arg1;
