#!/bin/bash
exec >/dev/null
. ~/.bashrc
export PATH=/usr/local/bin:$PATH
function do_it() {
    mkbeagleidx 
    mkgtags -i
    touch .for-code-reading
}

for x in `locate .for-code-reading`; do 
    cd `dirname $x` && do_it;
done
