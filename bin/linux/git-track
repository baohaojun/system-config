#!/bin/bash
set -e
. ~/.bash_profile
test $# == 0 && set -- `tbranch`
git co -t -b "$1" `git remote`/"$1" || (git config branch."$1".merge refs/heads/"$1"; git config branch."$1".remote `git remote`)
