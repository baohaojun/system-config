#!/usr/bin/python
import pynliner
import sys
import os
import re

str = open(sys.argv[1]).read()

str = str.replace(".footpara:nth-child(2) { display: inline; }", "")
str = pynliner.fromString(str)
sys.stdout.write(str.encode('utf-8'))


