#!/bin/bash

if test $# = 0; then
    set -- "$(xclip -o)"
fi

new_tab=-new-tab

if firefox-search-for-tab 'localhost.*/dict'; then
    new_tab=
fi
firefox $new_tab http://localhost:34567/dict/"$(echo "$@" | urlify)"
