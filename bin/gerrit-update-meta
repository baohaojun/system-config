#!/bin/bash
set -e

if test $# != 0; then
    mkdir -p ~/tmp/$1
    cd ~/tmp/$1
    git-get -u "$1" -d . || true
fi

git fetch $(repo-remote) refs/meta/*:refs/meta/*
git co refs/meta/config
start_recursive_shell update-meta
