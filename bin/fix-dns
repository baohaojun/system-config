#!/bin/bash
sudo /etc/init.d/network-manager restart
old_resolv_conf=$(
    cat /etc/resolv.conf
               )
cat <<EOF | sudo tee /etc/resolv.conf
nameserver 8.8.8.8
nameserver 4.4.4.4
$old_resolv_conf
EOF
