#!/bin/bash

set -e
function die() {
    echo Error: "$@"
    exit -1
}

if test "$#" != 1; then
    die "Error: Usage $(basename $0) java_file"
fi

global-ctags '.*' "$1" | grep -P -e 'package:\t' | pn 1
