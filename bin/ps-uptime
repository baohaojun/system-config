#!/bin/bash
set -x
ps -eo pid,etime,cmd|grep $1|grep -v -e "grep $1"|grep -v -P "^\s*$$\b"|tee /dev/stderr|pn 2|perl -ne 'next if m/^00:00$/; if (m/(\d*?)?:?(\d*):(\d*)$/) { print ($1 * 3600 + $2 * 60 + $3) }'
