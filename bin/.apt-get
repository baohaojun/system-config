#!/bin/bash
function apt-get() {
 sudo ifconfig eth0:0 $(ip-or-name r66) || sudo ifconfig wlan0:0 $(ip-or-name r66)
 ssh -R :9999:mirrors.163.com:80 localhost bash -c "true; cd $PWD; sleep 1; apt-get $@"
}
