#!/bin/bash

distr=${1:-debian}

if test $distr = debian; then
    release=${2:-sid}
else
    release=${2:-hardy}
fi

section=${3:-main}
wget -N http://cn.archive.ubuntu.com/${distr}/dists/${release}/${section}/source/Sources.bz2 -O Sources.$distr.$release.$section.bz2
