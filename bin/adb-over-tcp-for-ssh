#!/bin/bash
set -e
adb disconnect
adb-over-tcp
remote=$(adb devices|grep :5555 | pn 1)
host=${remote%:5555}
adb disconnect

ssh $1 -R 5555:$host:5555
ssh-exp $1 bash -c 'the-true-adb connect localhost'
