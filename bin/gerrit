#!/bin/bash
host=$( (repo-review-url $(repo-remote) | perl -npe 's/:.*//') 2>/dev/null )
if test -z "$host"; then
    host=bibler
fi

if test "$BPE_ON_APSE"; then
    host=bibler
fi

ssh $host gerrit "$@"
