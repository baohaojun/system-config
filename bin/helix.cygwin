#!/bin/bash
export BUILD_ROOT=/c/src_helix_cygwin/build
export PATH=$BUILD_ROOT/bin:"$PATH"
export SYSTEM_ID=linux-2.2-libc6-i586

export CVS_RSH=ssh
cd $BUILD_ROOT/../helix
if [[ $# == 0 ]]; then
   bash
else 
    "$@"
fi

