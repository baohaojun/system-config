#!/bin/bash
. ~/.bashrc
if test -e ~/.config/ssh-agent; then
    . ~/.config/ssh-agent;
fi

(
    cd ~/src/android
    ~/bin/repo sync -d -j4 -f
    repo-changes\?
)

if test "$RUNNING_IN_CRON" = true; then
    cron-code-reading
fi
