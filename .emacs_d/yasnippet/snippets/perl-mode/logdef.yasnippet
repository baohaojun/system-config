# -*- mode: snippet -*-
# key: logdef
# name: logdef
# --
open(my \$debug, ">", glob("~/.logs/`(buffer-name)`.log"))
    or die "Can not open debug log file ~/.logs/`(buffer-name)`.log";
sub debug(@) {
    print \$debug "@_\n";
}
