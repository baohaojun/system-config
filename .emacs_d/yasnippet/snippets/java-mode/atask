# -*- mode: snippet -*-
# name: atask
# key: atask
# --
    private class $1 extends AsyncTask<${2:String}, ${3:String}, ${4:String}> {
        @Override
            protected void onPreExecute() {
            //mTitleBar.startAnimation(mSlideIn);
        }

        @Override
            protected String doInBackground($2... args) {
                // publishProgress("");
                return "";
        }

        @Override
        protected void onProgressUpdate($3... args) {
            // String searchWord = args[0];
            // setEntryTitle(searchWord);
        }

        /**
         * When finished, push the newly-found entry content into our
         * {@link WebView} and hide the {@link ProgressBar}.
         */
        @Override
        protected void onPostExecute($4 ${5:parsedText}) {
            //mTitleBar.startAnimation(mSlideOut);
            //mProgress.setVisibility(View.INVISIBLE);

            //setEntryContent($5);
        }
    }