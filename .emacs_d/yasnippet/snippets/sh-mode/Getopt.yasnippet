# -*- mode: snippet -*-
# name: Getopt
# --
TEMP=\$(getopt -o ${1:j:} -n \$(basename \$0) -- "\$@")
eval set -- "\$TEMP"
while true; do
    case "\$1" in
	${2:-j})
	    ${3:jobs}=\$2
	    shift 2
	    ;;
	--)
	    shift
	    break
	    ;;
	*)
	    die "internal error"
	    ;;
    esac
done